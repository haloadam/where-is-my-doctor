# Re-derivable / large — kept out of git
raw/
interim/
dist/
# OSRM working dir: Hungary PBF + preprocessed graph (v2 routing)
osrm/
# Big tiler-input GeoJSON is regenerable from the pipeline; commit the pmtiles instead.
processed/*.geojson

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# OS
.DS_Store
